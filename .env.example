# Optional. Only read at build time, to lift GitHub's 60 requests/hour
# anonymous API limit while prerendering the release data. The site works
# without it — the build falls back to app/data/releases-snapshot.json.
GITHUB_TOKEN=

# Overrides the canonical URL. Defaults to the value in shared/site.ts.
NUXT_PUBLIC_SITE_URL=
